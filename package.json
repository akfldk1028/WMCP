{
  "name": "wmcp-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "WebMCP business projects monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "puppeteer-core": "^24.38.0",
    "vitest": "^4.0.18"
  }
}
